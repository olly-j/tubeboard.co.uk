{
  "name": "tubeboard-website-service",
  "version": "1.4.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "dev": "LIVE_ACTIVITY_WORKER_ENABLED=false node server/index.js",
    "test": "node --test",
    "check": "npm test && node --check server/index.js && node --check server/live-activity.js && node --check server/disruption-alerts.js && node --check server/status-monitor.js && node --check server/version.js && node --check site-20260724.js && scripts/check-repository.sh"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@apple/app-store-server-library": "3.1.0"
  }
}
